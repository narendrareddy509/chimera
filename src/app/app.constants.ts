import { environment } from '@env/environment';

export const CONSTANT = {
  GENERIC: {
    PERCENT_TO_DECIMAL: environment.constants.GENERIC.PERCENT_TO_DECIMAL,
  },
  PLAN_OPTIONS: {
    LEVEL_LIMIT: environment.constants.PLAN_OPTIONS.LEVEL_LIMIT,
  }
};
